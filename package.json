{
  "name": "discordbotmoments",
  "version": "1.0.0",
  "description": "A discord bot that store or show random images from: databases or random urls",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "Igor Nunes",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.3"
  },

  "engines": {
    "node": ">=12.0.0"
  }
 
  
  
}
